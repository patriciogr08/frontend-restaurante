// src/app/core/icons.ts
import { addIcons } from 'ionicons';
import {
  // Tabs / navegación
  homeOutline, speedometerOutline, analyticsOutline, statsChartOutline,
  listOutline, reorderFourOutline,
  fastFoodOutline, cubeOutline, pricetagOutline,
  peopleOutline, personAddOutline, personCircleOutline,

  // Acciones comunes
  createOutline, pencilOutline, addOutline, removeOutline, trashOutline,
  refreshOutline, settingsOutline, optionsOutline, funnelOutline, searchOutline,
  closeOutline, checkmarkOutline, checkmarkDoneOutline,
  downloadOutline, shareOutline,

  // Estado / feedback
  checkmarkCircleOutline, closeCircleOutline, alertCircleOutline,
  informationCircleOutline, warningOutline, powerOutline,

  // Auth / seguridad
  lockClosedOutline, keyOutline, logOutOutline, logInOutline, eyeOutline, eyeOffOutline,

  // Finanzas / ventas
  cashOutline, cardOutline, receiptOutline, walletOutline,

  // Archivos / medios
  cloudUploadOutline, cloudDownloadOutline, imageOutline, cameraOutline,
  documentTextOutline,

  // Tiempo / ubicación / comunicación
  calendarOutline, timeOutline, pinOutline, callOutline, mailOutline,
  chatbubbleEllipsesOutline,

  // Chevrons / UI
  chevronBackOutline, chevronForwardOutline, chevronDownOutline, chevronUpOutline,
} from 'ionicons/icons';

addIcons({
  // Tabs / navegación
  homeOutline, speedometerOutline, analyticsOutline, statsChartOutline,
  listOutline, reorderFourOutline,
  fastFoodOutline, cubeOutline, pricetagOutline,
  peopleOutline, personAddOutline, personCircleOutline,

  // Acciones
  createOutline, pencilOutline, addOutline, removeOutline, trashOutline,
  refreshOutline, settingsOutline, optionsOutline, funnelOutline, searchOutline,
  closeOutline, checkmarkOutline, checkmarkDoneOutline,
  downloadOutline, shareOutline,

  // Estado
  checkmarkCircleOutline, closeCircleOutline, alertCircleOutline,
  informationCircleOutline, warningOutline, powerOutline,

  // Auth
  lockClosedOutline, keyOutline, logOutOutline, logInOutline, eyeOutline, eyeOffOutline,

  // Finanzas
  cashOutline, cardOutline, receiptOutline, walletOutline,

  // Archivos / medios
  cloudUploadOutline, cloudDownloadOutline, imageOutline, cameraOutline,
  documentTextOutline,

  // Tiempo / ubicación / comunicación
  calendarOutline, timeOutline, pinOutline, callOutline, mailOutline,
  chatbubbleEllipsesOutline,

  // Chevrons
  chevronBackOutline, chevronForwardOutline, chevronDownOutline, chevronUpOutline,
});
